# Operator Language Intent Contract V2

Contract nguồn cho AI Client CNC:

```text
natural operator phrase
→ normalized language signals
→ structured machining intent
→ verified context resolution
→ deterministic process plan
→ teacher rubric / bounded student
→ static + geometry validation
→ VALIDATED_DRAFT
```

Không có bước nào được tạo production approval hoặc run permission.

Xem implementation:

- `edge_backend/ai/operator_language.py`
- `edge_backend/ai/intent_context_resolver.py`
- `edge_backend/ai/process_planner.py`
- `edge_backend/ai/agentic_gcode_response.py`
- `edge_backend/ai/agentic_response_harness.py`
