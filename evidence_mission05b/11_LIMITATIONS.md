# Limitations

- Sandbox does not run MATLAB/Simulink, NX MCD or real FluidNC.
- The test suite proves syntax/static/mock behavior, not hardware connectivity.
- `.env` with real secrets is excluded from the final package; use `config/.env.*.example` profiles.
